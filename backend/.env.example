# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas — replace with your own connection string, never commit real credentials
MONGO_URI=mongodb+srv://adarshkumar952530_db_user:qoJK9IzHZfID47Kq@cluster0.gltbhbs.mongodb.net/?appName=Cluster0

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS) — comma-separate multiple origins if needed
# Local dev:
CLIENT_URL=http://localhost:5173
# Production example:
#

# Library defaults
MAX_SEATS=100
DEFAULT_MONTHLY_FEE=800
