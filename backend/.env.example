# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas — replace with your own connection string, never commit real credentials
MONGO_URI=mongodb+srv://adarshkumar952530_db_user:qoJK9IzHZfID47Kq@cluster0.gltbhbs.mongodb.net/?appName=Cluster0

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS and Password Reset Links)
CLIENT_URL=http://localhost:5173

# Email Service (Nodemailer SMTP Configuration)
# For Gmail: host=smtp.gmail.com, port=587, password=16-digit Gmail App Password (NOT your account password)
# For Mailtrap (Dev testing): host=sandbox.smtp.mailtrap.io, port=2525
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password
EMAIL_FROM="Mishra Library ERP" <noreply@mishralibrary.com>

# Library defaults
MAX_SEATS=100
DEFAULT_MONTHLY_FEE=800
